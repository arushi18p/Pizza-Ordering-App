<?xml version="1.0" encoding="utf-8"?>
<inset xmlns:android="http://schemas.android.com/apk/res/android"
    >
    <layer-list>
        <!-- Background color -->
        <item>
            <shape android:shape="rectangle" >
                <corners android:radius="16dp" />
                <solid android:color="@color/AccentColorLightBlue" /> <!-- Change to your desired background color -->
            </shape>


        </item>

        <!-- Border -->
        <item>
            <shape android:shape="rectangle">
                <corners android:radius="16dp" />
                <stroke
                    android:width="2dp"
                    android:color="#FFFFFF" /> <!-- Change to your desired border color -->
            </shape>
        </item>
        <item android:drawable="@drawable/arrow_down" android:gravity="end|center_vertical" android:width="40dp" android:height="40dp"/>

    </layer-list>
</inset>
